{
  "name": "hospital-pms-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
